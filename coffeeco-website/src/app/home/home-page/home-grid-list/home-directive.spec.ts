import { HomeItemDirective } from './home-directive';

describe('HomeItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeItemDirective();
    expect(directive).toBeTruthy();
  });
});
